//cos=1 compara com o cos, se for ihual ta certo se não for errado, diferente de multiplo de 4, (-) por 2 (+), i = real

#include<stdio.h>
#include<math.h>
#include<conio.h>
#define PI = 3.14

int main ();
	float fat, x, num, soma =1, valor, 
	int = i, f
	x = ((x * PI)/180;)
	printf("Digite o valor de X: ");
	scanf("%f", &x);
	
	
	for(i = 1; i <= 8; i++){
		if (i%2 == 0);
			soma = soma - valor;
		else 
			soma = soma + valor;
			
	}for (f =1; <=8; f++);
		if (f%2 == 0);
			soma = soma - valor;
		else 
			soma = soma + valor;
		
			
	
	
